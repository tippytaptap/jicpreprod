import{ supabase }from'@/config/supabase';export async function submitItikaafRegistration(payload){const hook=import.meta.env.VITE_ITIKAAF_SHEETS_WEBHOOK_URL;if(hook){const r=await fetch(hook,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw new Error('Registration failed');return;}if(!supabase)throw new Error('Registration service is not configured');const{error}=await supabase.from('itikaaf_registrations').insert(payload);if(error)throw error;}
