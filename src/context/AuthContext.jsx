import React,{createContext,useContext,useEffect,useMemo,useState} from 'react';
import { supabase } from '@/config/supabase';
const C=createContext({});
export function AuthProvider({children}){const[session,setSession]=useState(null);const[loading,setLoading]=useState(true);const[role,setRole]=useState(null);useEffect(()=>{if(!supabase){setLoading(false);return;}supabase.auth.getSession().then(({data})=>{setSession(data.session);setLoading(false)});const{data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);useEffect(()=>{if(!supabase||!session?.user){setRole(null);return;}supabase.from('profiles').select('role').eq('id',session.user.id).maybeSingle().then(({data})=>setRole(data?.role||null))},[session]);const value=useMemo(()=>({session,user:session?.user||null,loading,role,isAdmin:role==='admin',signIn:(email,password)=>supabase?.auth.signInWithPassword({email,password}),signOut:()=>supabase?.auth.signOut()}),[session,loading,role]);return <C.Provider value={value}>{children}</C.Provider>};export const useAuth=()=>useContext(C);
