# ============================================================
#  ENVIRONMENT VARIABLES  —  .env.example
# ============================================================
#  1. Copy this file:  cp .env.example .env
#  2. Fill in your values below
#  3. NEVER commit .env to git  (it's already in .gitignore)
# ============================================================

# Supabase — found in: supabase.com → your project → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Google Sheets webhook for Itikaaf registrations
# If blank, registrations go to Supabase instead
VITE_ITIKAAF_SHEETS_WEBHOOK_URL=

# ── CMS / Admin ────────────────────────────────────────────────
# After running supabase/cms_schema.sql, create an admin user at:
#   supabase.com → your project → Authentication → Users → Add user
# Use that email/password to log in via the key icon in the navbar.
